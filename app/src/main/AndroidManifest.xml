<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alamat.todolist">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_iconapp"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_iconapp"
        android:supportsRtl="true"
        android:theme="@style/Theme.ToDoList">
        <activity android:name=".UpdateToDoList"></activity>
        <activity android:name=".SaveToDoList" />
        <activity android:name=".MainAppActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>